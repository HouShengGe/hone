<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="@dimen/volume_popupwindow_width"
    android:layout_height="@dimen/volume_popupwindow_height"
    android:background="@drawable/bg_volume_popupwindow"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:orientation="vertical">
        <SeekBar
            android:max="100"
            android:id="@+id/volumeSeekBar"
            android:layout_width="@dimen/volume_seekbar_width"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:rotation="270" />
    </LinearLayout>

    <TextView
        android:id="@+id/volumeTv"
        android:textStyle="bold"
        android:textSize="20sp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:textColor="@color/colorAccent"
        android:text="90" />
</LinearLayout>